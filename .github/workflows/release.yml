name: "Tauri Windows Release (Dynamic, Auto Version, PEM Fix)"

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  release:
    runs-on: windows-latest
    steps:
      # 1️⃣ Checkout
      - name: 🛒 Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Node.js & Rust
      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 🦀 Setup Rust
        uses: dtolnay/rust-toolchain@stable

      # 3️⃣ Install dependencies
      - name: 🪟 Install dependencies
        run: |
          npm install -g yarn
          yarn install

      # 4️⃣ Write raw private key as PEM (Fixed Multiline Handling)
      - name: 🗝️ Write private key to PEM file
        shell: pwsh
        run: |
          # Convert literal \n to newlines for PEM format
          $Key = "${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}" -replace '\\n', "`n"
          $KeyPath = "src-tauri/signing_private.pem"
          Set-Content -Path $KeyPath -Value $Key
          echo "TAURI_SIGNING_PRIVATE_KEY=$(Get-Content $KeyPath -Raw)" >> $env:GITHUB_ENV

      # 5️⃣ Auto-increment tag/version
      - name: 🔖 Auto increment version tag
        id: version
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $tags = git tag --list | Sort-Object { [version]($_ -replace '^v', '') } -Descending
          if ($tags.Length -eq 0) {
            $newTag = "v0.1.0"
          } else {
            $latestTag = $tags[0]
            Write-Host "Current tag: $latestTag"
            $version = [version]($latestTag -replace '^v', '')
            $newVersion = [version]::new($version.Major, $version.Minor, $version.Build + 1)
            $newTag = "v$($newVersion.ToString())"
          }

          Write-Host "New tag: $newTag"
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          git tag -d $newTag 2>$null
          git push origin :refs/tags/$newTag 2>$null

          git tag $newTag
          git push origin $newTag

          echo "VERSION=$newTag" >> $env:GITHUB_ENV

      # 6️⃣ Build Tauri app
      - name: 🚀 Build Windows release with Tauri
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ env.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: "${{ env.VERSION }}"
          releaseName: "Windows Release ${{ env.VERSION }}"
          releaseBody: "Windows installer and signature"
          releaseDraft: false
          prerelease: false
          includeUpdaterJson: false

      # 7️⃣ Create latest.json for updater
      - name: 🧾 Generate latest.json
        shell: pwsh
        run: |
          $version = "${{ env.VERSION }}"
          $date = Get-Date -Format "yyyy-MM-ddTHH:mm:ssZ"
          $url = "https://github.com/${{ github.repository }}/releases/download/$version/Siri Billing App_${version}_x64-setup.exe"
          $sig_url = "https://github.com/${{ github.repository }}/releases/download/$version/Siri Billing App_${version}_x64-setup.exe.sig"

          $latestJson = @{
            version = $version
            date = $date
            notes = "Windows release $version"
            platform = @{
              windows = @{
                url = $url
                signature = $sig_url
              }
            }
          } | ConvertTo-Json -Depth 5

          $latestJson | Out-File -FilePath latest.json -Encoding UTF8

      # 8️⃣ Upload all artifacts dynamically
      - name: 📤 Upload release assets
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh auth setup-git

          $version = "${{ env.VERSION }}"

          # Ensure release exists
          try { gh release view $version -R $env:GITHUB_REPOSITORY | Out-Null } catch {
              Write-Host "Release $version does not exist, creating..."
              gh release create $version --title "Windows Release $version" --notes "Automated release from workflow" -R $env:GITHUB_REPOSITORY
          }

          # Upload artifacts
          $folders = @(
              "src-tauri/target/release/bundle/msi",
              "src-tauri/target/release/bundle/nsis"
          )
          foreach ($folder in $folders) {
              if (Test-Path $folder) {
                  Get-ChildItem -Path $folder -File | ForEach-Object {
                      Write-Host "Uploading $($_.FullName)..."
                      gh release upload $version $_.FullName --clobber -R $env:GITHUB_REPOSITORY
                  }
              }
          }

          # Upload latest.json
          gh release upload $version latest.json --clobber -R $env:GITHUB_REPOSITORY

      # 9️⃣ Update tauri.conf.json version
      - name: 🔄 Update tauri.conf.json version
        shell: pwsh
        run: |
          $config = Get-Content "src-tauri/tauri.conf.json" -Raw | ConvertFrom-Json
          $config.package.version = "${{ env.VERSION }}" -replace '^v',''
          $config | ConvertTo-Json -Depth 10 | Out-File "src-tauri/tauri.conf.json" -Encoding utf8

          git add src-tauri/tauri.conf.json
          git commit -m "🔄 Update Tauri version to ${{ env.VERSION }}"
          git push origin main
